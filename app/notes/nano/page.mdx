## nano quickstart

nano is an often underrated text editor which is simple enough and intuitive to use. Ideal when you don't remember vim key bindings. Most of the shortcuts are present in the tui itself.

```
`esc a` to mark
`alt+{` to intent
`alt+u` to undo
```