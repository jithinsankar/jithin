## tmux quickstart

tmux is a great tool to get work done on while working in headless environments especially in EC2 servers. Tmux can be used to manage sessions, split windows and much more.

`ctrl+b %` split vertically \
`ctrl+b “` split horizontaly \
`ctrl+b arrow` to switch \
`ctrl+b page down` to scroll \